# Repository Memory

- After every code, content, or configuration change, run `bundle exec jekyll build --baseurl /al-folio` from the repository root before reporting completion.
- If the build cannot run because the local environment is missing required tools or versions, report the exact command attempted, the failure reason, and any remediation attempted.
- Do not claim a change is fully verified unless the build command succeeds.
